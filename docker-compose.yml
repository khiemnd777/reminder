services:
  reminder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reminder
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
    restart: unless-stopped
